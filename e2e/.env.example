# URL фронтенда (main-app, Vite dev server по умолчанию).
BASE_URL=http://localhost:5173

# URL бэкенда для API-сидинга и быстрых проверок.
API_URL=http://localhost:8000

# Mailpit HTTP API (для чтения писем в тестах восстановления пароля и т.п.).
MAIL_URL=http://localhost:8025

# CI=1 включает retries и отключает слежку за изменениями.
# CI=1
