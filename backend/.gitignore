# Binaries
/bin/
/tmp/
tmp/
*.exe
*.dll
*.so
*.dylib

# Air
build-errors.log

# Test / coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Local env (real secrets live in k8s; .env is dev-only and never committed — see .env.example)
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Build artifacts
/dist/
/build/
